dashboard:
  enabled: true
  host: 0.0.0.0
  port: 8000
  auto_reload: false
  authentication:
    enabled: false
    type: basic
  endpoints:
    health: /health
    positions: /positions
    performance: /performance
    config: /config
    logs: /logs
    emergency_stop: /emergency-stop
  websocket:
    enabled: true
    port: 8001
    heartbeat_interval: 30
  refresh_rates:
    positions: 1
    performance: 5
    system_health: 10
